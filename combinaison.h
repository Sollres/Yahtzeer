#include <stdio.h>
#include <stdlib.h>

void nb_face(t_main *);
void totaux(t_main );
int brelan(t_main ,t_fiche_score *);
int carre(t_main ,t_fiche_score *);
void full(t_main ,t_fiche_score *);
void pte_suite(t_main , t_fiche_score *);
void gde_suite(t_main , t_fiche_score *);
void yams(t_main , t_fiche_score *);
void chance(t_main , t_fiche_score *);
void points(t_main ,t_fiche_score *, int ,int );
int f_combinaison(t_main ,t_fiche_score *);