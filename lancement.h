#include <stdio.h>
#include <stdlib.h>
#include <time.h>

typedef struct s_main
{
    int de1, de2, de3, de4, de5;
    int nb_un, nb_deux, nb_trois, nb_quatre, nb_cinq, nb_six;
}t_main;

void lancer(t_main *, int);